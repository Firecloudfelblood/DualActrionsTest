name: Viewer Automation Tests
on:
  #this allows to run manually
  workflow_dispatch:
    inputs:
      Env:
        description: Environment to run
        default: dev
        required: true
jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Comment the run
        run: echo "Test Manually runned"
